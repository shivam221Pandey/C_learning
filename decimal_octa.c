#include<stdio.h>
int main(){
    int a, j, i, result, arr[10];
    printf("Enter binary number to convert in octal");
    scanf("%d", &a);
    while(a != 0){
        result = a%8;
        arr[i] = result;
    }
}